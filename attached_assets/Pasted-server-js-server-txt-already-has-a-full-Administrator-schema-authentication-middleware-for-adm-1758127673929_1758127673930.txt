server.js (__ server.txt) already has a full Administrator schema, authentication middleware for admins, and JWT handling

__ server

. That means your backend is ready to issue tokens to admins and protect admin routes.

admin.html (admin.txt) is a static front-end dashboard template

admin

. Right now it’s disconnected: the numbers and tables are hardcoded instead of being powered by your backend.

To “fully integrate” the admin page into the server, here’s what you need to do step by step:

1. Serve the Admin Frontend

In your server.js, add a route to serve admin.html:

const path = require('path');

app.get('/admin', (req, res) => {
  res.sendFile(path.join(__dirname, 'admin.html'));
});

2. Build Admin Authentication

You already have an Administrator model. Add login routes:

app.post('/login-admin', async (req, res) => {
  const { email, password } = req.body;
  try {
    const admin = await Administrator.findOne({ email });
    if (!admin) return res.status(400).json({ message: 'Invalid credentials' });

    const isMatch = await bcrypt.compare(password, admin.password);
    if (!isMatch) return res.status(400).json({ message: 'Invalid credentials' });

    const token = jwt.sign({ id: admin._id, role: 'admin' }, JWT_SECRET, { expiresIn: '1h' });
    res.json({ token });
  } catch (err) {
    res.status(500).json({ message: 'Server error' });
  }
});


Your authenticateAdminToken middleware (already present) will protect endpoints like /admin/dashboard-data.

3. Expose Dashboard APIs

Replace hardcoded stats in admin.html with backend data. Example:

app.get('/admin/dashboard-data', authenticateAdminToken, async (req, res) => {
  const totalStudents = await Student.countDocuments();
  const totalTeachers = await Teacher.countDocuments();
  const totalQuizzes = await QuizQuestion.countDocuments();
  const totalNotes = await WorkFile.countDocuments();
  const pendingApproval = await Activity.countDocuments({ approved: false });

  res.json({ totalStudents, totalTeachers, totalQuizzes, totalNotes, pendingApproval });
});

4. Connect Frontend to API

In admin.html, use JavaScript (with fetch) to call these endpoints after login:

<script>
async function loadDashboard() {
  const token = localStorage.getItem('adminToken');
  const res = await fetch('/admin/dashboard-data', {
    headers: { Authorization: `Bearer ${token}` }
  });
  const data = await res.json();

  document.querySelector('.stat-card .value').innerText = data.totalStudents;
  // repeat for teachers, quizzes, notes, etc.
}
window.onload = loadDashboard;
</script>

5. Secure Routes

Protect user management (/admin/students, /admin/teachers) with authenticateAdminToken.

Add CRUD endpoints for approving/rejecting quizzes, notes, etc.

👉 So the big picture: your backend already supports admins; your frontend needs to fetch real data from it, and you need a login system that stores an admin token in localStorage and uses it for protected API calls.

Do you want me to draft the admin login flow (frontend form + backend route + token storage), or should I first help you wire the dashboard numbers dynamically?